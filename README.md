# Todo List App

A simple React app that displays a list of todos using Vite.

## Installation

1. Install dependencies:

   ```bash
   npm install
   ```

2. Run the development server

   Start the app locally:

   ```bash
   npm run dev
   ```

## To run the development server

Open link in your browser:

```bash
http://localhost:5173
```
